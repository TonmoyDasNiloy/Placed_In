# MongoDB connection string
MONGODB_URL=mongodb://localhost:27017/Placed_In
# Server port (optional, defaults to 8800)
PORT=8800

# JWT secret key — use any long random string
JWT_SECRET_KEY=your_super_secret_jwt_key_here

# Gmail credentials for nodemailer (email verification + password reset)
AUTH_EMAIL=tonmoydas070@gmail.com
AUTH_PASSWORD=wdbxjlgoybriumdc

# Base URL of your server (used to build verification/reset links)
APP_URL=http://localhost:8800/